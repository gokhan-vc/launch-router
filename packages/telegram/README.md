# Telegram skin

`/pads`, `/caps <pad>`, `/draft`, `/simulate` draft an intent. **Signing is a Mini App** (Privy or WalletConnect). The Bot API cannot hold a wallet.

v0: command parser only. Mini App UI is not shipped.
